rootProject.name='conductor'
include 'correlation', 'common', 'core', 'redis-persistence','es5-persistence','es5rest-persistence', 'jersey', 'client', 'test-harness', 'ui', 'contribs', 'server'
rootProject.children.each {it.name="conductor-${it.name}"}
